#include "main.h"

/**
 * _is upper - check if c is upper
 *
 * @c: input for alphabet
 *
 * Return 1 if it upper case, 0 if it not
*/

int _isupper(int c)

	if (c >= 65 && <= 90) 
	return (1);
	else
	return (0);
